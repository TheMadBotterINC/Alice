<div class="max-w-4xl mx-auto">
  <div class="mb-6">
    <h1 class="text-3xl font-bold text-gray-900">Edit Dataset</h1>
    <p class="mt-2 text-sm text-gray-600">
      Update the configuration for <span class="font-semibold"><%= @dataset.name %></span>.
    </p>
  </div>

<%= render "form", dataset: @dataset, table_schema: nil %>
</div>
